import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#06B3B7',
    },
    secondary: {
      main: '#F73420',
      light: '#6BF2F2',
    },
  },
});

export default theme;
